"""NAV-Erosion engine package.

Holds the model engine, scoring, classifier, data fetcher, and the per-ticker
screener. Root entry points (run.py) add this folder to sys.path and
import from here; the scripts are also runnable directly (e.g.
``python src/build_board.py``).

Educational research framework, not investment advice.
"""
